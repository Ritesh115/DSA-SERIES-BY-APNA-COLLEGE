#include <iostream>
using namespace std;

int main()
{
  // conditional statments
  // if ()
  // if-else()
  // else-if() ladder => when we want to check multiple conditions
  // nested if()
  // Switch-case

  // terniary operator => short form if else()
  int age = 6;
  cout << (age > 18 ? "give vote" : "no vote") << endl;
  cout << endl;

  return 0;
}
